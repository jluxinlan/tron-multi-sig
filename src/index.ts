/**
 * TRON Multi-Signature Service Demo
 * Entry point - This is a demo implementation for reference.
 * Developers should build their own SDK based on their specific requirements.
 */

export { MultiSigClient } from './services/MultiSigClient';
export * from './types';
export { generateSign, generateUUID, generateAuthParams } from './utils/signature';
